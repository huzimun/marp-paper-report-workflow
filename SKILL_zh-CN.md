# Marp 论文汇报工作流

> 说明：这是 `SKILL.md` 的中文检查版，便于人工审阅。Codex 的技能触发仍以英文 `SKILL.md` 为准。

## 目标

将基于论文的 Marp/Markdown PPT 工作变成一套可核查、可迭代、尽量自动化的流程：用户只提出需求并审批关键结果；Codex 负责证据追溯、页面修改、图表生成或提取、PDF 编译预览，以及迭代记录。

## 核心规则

- 保护用户原稿。不要直接覆盖源 PPT；先创建带版本号的工作副本和输出文件。
- 只修改用户明确指定的页面，或包含 `图表占位` 等明确占位符的页面；除非用户批准扩大修改范围。
- 避免泄露隐私。skill 和通用记录中不要写入个人姓名、本地用户目录、课程私有路径、账号凭据或私有链接。
- 优先保证证据可追溯，而不是只追求表达顺滑。PPT 中保留的每条事实性表述都应被原文支持、修正、标记为推演，或标记为暂未解决。
- 将 PDF 作为最终预览和导出成果。HTML/CSS/SVG 只作为 Marp 内部绘图方式；独立 HTML 只在检查或调试时保留。
- 用户职责保持简洁：提出需求、补充缺失论文、确认关键事实、审批最终图文效果。

## 工作流程

1. 创建版本化工作区：
   - 识别源 `.md` 或 `.marp.md` 文件。
   - 创建项目本地输出目录，例如 `workflow_outputs/`，下设 `papers/`、`figures/`、`artifacts/`、`versions/`。
   - 编辑前先复制源 PPT，生成带时间戳或版本号的工作文件。
   - 每轮正式迭代都写入 `workflow_outputs/version_log.md` 或等价的项目本地版本日志，记录版本、修改页码、修改原因、执行命令、输出文件、视觉检查和审批状态。

2. 建立证据库：
   - 从 PPT 和会议纪要中识别引用论文。
   - 下载论文 PDF，或使用用户提供的 PDF。
   - 如果出版社官网下载失败，可以使用 arXiv 或其他公开仓库，但必须核对标题、作者、年份、DOI/arXiv 编号和版本是否一致。
   - 建立论文清单：标题、作者、期刊/会议、年份、DOI/arXiv、来源链接、本地文件名、核验状态。
   - 每下载或接收一篇论文 PDF，都生成一段 150 到 300 字的中文简介，写入 `workflow_outputs/paper_summaries.md`。简介应包含研究问题、方法或实验设计、核心发现、与 PPT 的关系，以及人工复核入口。
   - 详细字段参考 `references/fact-checking.md`。

3. 逐页事实核查：
   - 按 PPT 页码逐页处理。
   - 对每条关键表述记录：PPT 页码、PPT 原文、对应论文、论文原文摘录、原文位置、核查结论和处理动作。
   - 将表述分类为：原文支持、需要修正、推演/拓展、暂未找到依据。
   - 为人工复核提供可直接操作的路径：本地 PDF 文件名、页码、章节/图表/表格、短摘录。

4. 修改图表与排版：
   - 实验结果和明确要求引用的图表，优先使用论文原图。
   - 原图过密时，可以裁剪关键区域或简化重绘，但必须保留原图来源说明。
   - 如果截图建议要求中文标注或局部说明，不要直接插入未处理原图，应生成带标注版本。
   - 图表说明必须用可追溯开头，例如 `Steyvers et al. Fig. 1：` 或 `Cheung et al. Table 1：`，不要用 `读图：` 这类泛称开头。
   - 图表说明必须能独立理解：只看图片和说明，就能知道图像表达的含义。
   - 概念图、机制图、流程图优先用 Marp 内嵌 HTML/CSS/SVG 绘制。
   - 需要更强视觉表现或插画感时，再生成位图并嵌入。
   - 每页来源说明统一放在 PPT 底部一行，格式为：
     `来源：Author et al., Paper Title, Venue, Year, https://...`
   - 版式和页面检查规则参考 `references/marp-layout.md`。

5. 编译和检查：
   - 优先验证 VS Code 本地 Marp 预览流程。
   - 尽量同时验证 Marp CLI 导出：将工作 Markdown 编译为 PDF，嵌入本地图片时启用本地文件访问。
   - 如果要求通用性或本地环境不可用，再验证在线编辑器路径。
   - 视觉检查代表性页面：边界留白、图片可读性、字体协调、来源脚注位置。

6. 验证自动化样例：
   - 至少选择 2 到 3 个代表页面跑通完整闭环：用户需求、Codex 修改、编译/预览、审批点、迭代、最终结果。
   - 如条件允许，覆盖论文原图页、HTML/生成图概念页、复杂图文混排页。
   - 每个样例必须记录具体页码、修改文件、执行命令、输出文件、审批点和返工次数。

7. 最终交付：
   - 带版本的 Marp Markdown 工作稿。
   - 生成的 PDF。
   - 论文原文清单。
   - 论文中文简介。
   - 带人工可复核信息的事实核查记录。
   - 带页码的自动化工作流验证记录。
   - Marp 编辑环境验证结论与故障切换路径。

## 版本管理

在进行大幅修改、多轮迭代，或涉及隐私与安全约束的任务前，先阅读 `references/versioning-and-safety.md`。不要覆盖已经确认的版本；每轮新修改都必须从最新确认的 Markdown/PDF 版本派生出下一个版本号。

## 参考文件

- 需要提取关键表述、建立论文清单、准备人工复核材料时，阅读 `references/fact-checking.md`。
- 需要修改 PPT、生成图表、验证 PDF 输出、比较 VS Code/CLI/在线 Marp 流程时，阅读 `references/marp-layout.md`。
- 需要设置输出目录、命名版本、记录迭代或清理隐私信息时，阅读 `references/versioning-and-safety.md`。
