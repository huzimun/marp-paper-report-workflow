# Marp 论文汇报工作流

> 说明：这是 `SKILL.md` 的中文检查版，便于人工审阅。Codex 的正式触发仍以英文 `SKILL.md` 为准。

## 目标

把论文型 Marp/Markdown PPT 工作变成一套可核查、可迭代、且按需求轻重分流的流程。Codex 应先选择满足用户请求的最轻模式，再处理页面修改、必要的证据追踪、PDF 预览和版本记录。

## 模式选择

执行任务前必须先选择一种模式，并简短说明选择理由。

### 快速填图模式

当用户已经提供所需图像、表格截图、裁剪图或其他可直接放入 PPT 的资产，并要求填入指定页或占位符时，默认使用本模式。

快速模式负责：

- 读取用户指定页面或显式占位符；
- 摘录该页原始图表要求、用途、截图建议和周围教学点；
- 确认用户提供的资产存在，并与指定论文、图号/表号或用户批准的替代图相匹配；
- 将图像/表格放入 Marp，不默认修改图片像素；
- 写简洁中文说明，开头使用 `Author et al. Fig. X:` 或 `Author et al. Table X:`；
- 添加或保留统一底部来源；
- 导出 PDF，并视觉检查修改页；
- 写简短版本日志，记录页面、资产、输出、视觉检查和审批状态。

快速模式默认不做：

- 下载论文；
- 从 PDF 抓图；
- 建立或刷新完整论文清单；
- 为所有论文生成中文摘要；
- 全稿逐页事实核查；
- 运行与当前修改无关的 workflow validation 样例。

只有在资产缺失或歧义、图表身份无法确认、说明需要图片外事实支撑，或用户明确要求下载论文、抓图、事实核查、完整审校时，才升级到完整论文审校模式。

### 完整论文审校模式

当用户要求下载或整理论文、从 PDF 提取图表、事实核查 PPT 陈述、生成论文摘要、审计引用、广泛修改论文依据内容，或快速模式无法安全完成时，使用本模式。

完整模式包含证据库、事实核查记录、论文清单、中文摘要、图表提取或裁剪、版本化页面修改、PDF 预览、视觉检查和 workflow 记录。

## 核心规则

- 保护用户原稿。不要直接覆盖源 PPT；先创建带版本号的工作副本和输出文件。
- 只修改用户明确指定的页面，或包含 `图表占位` 等显式占位符的页面；除非用户批准扩大范围。
- 将每页原始 `用途`、`截图建议` 和指定图表视为硬性要求。不得因为整图方便、重绘美观或生成图顺手而替代原要求，除非用户明确批准。
- 避免隐私泄露。skill 和通用记录中不要写入个人姓名、本地用户目录、课程私有路径、账号凭据或私有链接。
- 完整审校模式下，PPT 中保留的事实性表述应有原文支持、被修正、标为推演，或标为暂未解决。
- PDF 是最终预览和导出成果。HTML/CSS/SVG 只作为 Marp 内部绘图方式；独立 HTML 只在检查或调试时保留。
- 用户职责保持简洁：提出需求、补充缺失论文、确认关键事实、审批最终图文效果。
- 所有可复用 Markdown 记录使用 UTF-8。若已有文件乱码，修改时应重写为可读 UTF-8。

## 图表占位需求 Gate

填充或替换任何图表占位前，必须先摘录并记录该页原始需求块：

- PPT 页码；
- `图表占位` 原文，包括论文名、图号或表号；
- `用途` 原文；
- `截图建议` 原文；
- 该页周围正文或教师备注中的论证目标；
- 选择的工作流模式：`fast_figure_fill` 或 `full_paper_audit`。

这个需求块是阻断式检查清单。最终选择的资产必须匹配指定论文图表、页面用途和截图建议；若有偏离，必须在版本日志中记录用户批准。

当用户提供图表图片时，在快速模式中将该文件视为选定资产。不要为了复制用户给出的图片而反向抓 PDF 或下载论文。保留用户图片作为可追踪资产，中文标签、callout 或导读优先用 Marp Markdown/HTML/CSS 放在图片周围；除非用户明确要求，否则不要改动图片像素。

完整模式下，Codex 提取的论文图表应保存干净完整版本。只有当页面要求仍然需要裁剪、且用户没有覆盖该要求时，才制作局部裁剪图。若当前材料无法满足需求块，应暂停并说明缺失资产或歧义，不要自行编造替代图。

## 快速填图流程

1. 确定范围：
   - 识别最新已批准或用户指定的基线版本。
   - 创建下一版工作 Markdown 和对应输出名。
   - 只修改用户指定页面和显式占位符。

2. 读取占位需求：
   - 捕捉页码、`图表占位`、`用途`、`截图建议` 和教学点。
   - 记录用户提供资产路径，以及它预期代表的图号/表号。

3. 插图与说明：
   - 直接使用用户提供的图片或表格资产。
   - 保证资产清晰、位于安全边距内。
   - 写独立可读的中文说明，开头用 `Author et al. Fig. X:` 或 `Author et al. Table X:`。
   - 添加统一来源：`来源：Author et al., Paper Title, Venue, Year, https://...`

4. 编译和检查：
   - 尽可能导出 PDF。
   - 对每个修改页做 PDF 或 PNG 视觉检查，检查边距、清晰度、重叠、字号协调和来源脚注位置。
   - 若 PDF 失败但 HTML 正常，记录失败，并只在用户接受或项目已验证时使用本地 fallback。

5. 记录和暂停：
   - 更新版本日志，记录页面、资产、命令、PDF、视觉检查、审批状态和未解决问题。
   - 用户要求“导出后先暂停”时，生成 PDF 后停止，不额外做完整审校。

## 完整论文审校流程

1. 创建版本化工作区：
   - 识别源 `.md` 或 `.marp.md`。
   - 创建 `workflow_outputs/`，包含 `papers/`、`figures/`、`artifacts/`、`versions/`。
   - 编辑前复制源 PPT 为版本化工作文件。
   - 每轮正式迭代写入 `workflow_outputs/version_log.md`。

2. 建立证据库：
   - 从 PPT 和会议纪要识别引用论文。
   - 下载论文 PDF 或使用用户提供 PDF；非出版社来源必须核对标题、作者、年份、DOI/arXiv 和版本。
   - 建立论文清单，记录标题、作者、期刊/会议、年份、DOI/arXiv、来源链接、本地文件名和验证状态。
   - 为每篇下载或接收的论文生成 150-300 字中文摘要，写入 `workflow_outputs/paper_summaries.md`。
   - 详细字段参考 `references/fact-checking.md`。

3. 事实核查：
   - 在用户批准范围内按页处理。
   - 对关键陈述记录 PPT 页码、原文、对应论文、原文摘录、位置、结论和处理动作。
   - 分类为：原文支持、需要修正、推演/拓展、暂未找到依据。
   - 提供本地 PDF 文件名、页码、章节/图表/表格和短摘录，便于人工复核。

4. 修改图表与排版：
   - 每次选择图像、裁剪、标注、重绘或生成图前，先执行图表占位需求 gate。
   - 实验结果和明确引用图表优先使用论文原图。
   - 原图过密时可裁剪关键区域或简化重绘，但必须保留原图来源说明。
   - 中文标注优先用 Marp Markdown/HTML/CSS 添加在图片周围，保持论文图片本身干净。
   - 图表说明必须能独立理解，并以可追踪开头开始。
   - 来源统一放在底部一行。

5. 编译和检查：
   - 优先验证 VS Code 本地 Marp 预览。
   - 尽可能验证 Marp CLI PDF 导出。
   - 只有在请求通用性或本地不可用时，才验证在线编辑器路径。
   - 检查边界、图片可读性、字体协调和来源脚注。

6. 仅在验证 skill 本身时运行自动化样例：
   - 普通快速填图任务不运行此步骤。
   - 当任务是测试或改进 skill 时，选择 2-3 个代表页面跑完整闭环。

7. 交付：
   - 带版本的 Marp Markdown。
   - PDF。
   - 完整模式产生或更新的论文清单、中文摘要、事实核查记录和 workflow validation 记录。
   - 相关 Marp 环境验证结论。

## 版本管理

大幅修改、多轮迭代或涉及隐私安全约束前，先阅读 `references/versioning-and-safety.md`。不要覆盖已批准版本；每轮新修改从最新已批准 Markdown/PDF 派生下一版本。

如果用户要求删除或重做版本，保留审计链：将旧版本在日志中标为 `rejected`、`deprecated` 或 superseded，再创建替代版本，除非用户明确要求清理未批准临时文件。

## 参考文件

- 提取关键表述、建立论文清单、准备人工复核材料，或判断快速模式是否需要升级时，阅读 `references/fact-checking.md`。
- 修改 PPT、插入用户图片、生成图表、验证 PDF 输出，或比较 VS Code/CLI/在线 Marp 流程时，阅读 `references/marp-layout.md`。
- 设置输出目录、命名版本、记录迭代、替换/拒绝版本或清理隐私信息时，阅读 `references/versioning-and-safety.md`。
